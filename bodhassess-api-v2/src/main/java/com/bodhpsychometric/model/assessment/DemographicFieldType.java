package com.bodhpsychometric.model.assessment;

/**
 * Input kind of a demographic field — the five types the old service already
 * validated ("type must be one of: text, number, date, select, textarea").
 */
public enum DemographicFieldType {
    TEXT,
    NUMBER,
    DATE,
    SELECT,
    TEXTAREA
}

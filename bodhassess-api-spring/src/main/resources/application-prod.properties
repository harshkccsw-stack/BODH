# ====== Prod profile — picks up DB_URL etc. from environment ======
spring.datasource.url=${DB_URL:jdbc:mysql://mysql:3306/bodhassess?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8}
spring.datasource.username=${DB_USERNAME:bodh}
spring.datasource.password=${DB_PASSWORD:bodh}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.show-sql=false

node_modules/
package-lock.json
# bodhassess-app/dist
*.tsbuildinfo
bodhassess-app/.env
bodhassess-app/.env.local
bodhassess-app/.env.development.local
bodhassess-app/.env.test.local
bodhassess-app/.env.production


bodhassess-portal/.env
bodhassess-portal/.env.local
bodhassess-portal/.env.development.local
bodhassess-portal/.env.test.local
bodhassess-portal/.env.production



target/

spring-social/target
.envss
# The ROOT .env is TRACKED (it holds non-secret local wiring and predates
# this line), so secrets must never be added to it. secrets.env is the
# untracked companion for anything that bills or authenticates — put
# OPENAI_API_KEY there, not in .env.
secrets.env
.env.secrets
.env.production
.env.development
docker-compose.prod.yml
docker-compose.dev.yml
docker-compose.override.yml

# Filled-in backup config / Spaces credentials live only on the droplet.
ops/bodh-backup.env
ops/.s3cfg*
